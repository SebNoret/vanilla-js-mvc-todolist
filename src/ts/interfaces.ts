abstract class abstractControler {
  onTodoListChanged = () => {};

  handleToggleTodo = (id) => {};

  handleAddTodos = (text) => {};
  handleDeleteDoneTodos = () => {};
  handleDisplayEditForm = (id) => {};

  handleEditTodo = (id, text) => {};

  displayComponent() {}
}

export default abstractControler;
